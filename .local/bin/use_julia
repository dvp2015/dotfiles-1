export JULIA_VERSION="1.0.0"

if [[ -n "$BASH_VERSION" ]]; then
    export PATH=$(make_path "/opt/julia-$JULIA_VERSION/bin" $PATH)
elif [[ -n "$ZSH_VERSION" ]]; then
    path=(
        /opt/julia-${JULIA_VERSION}/bin
        $path
    )
fi
