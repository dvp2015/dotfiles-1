#!/bin/bash

. ~/.bash-functions.rc

download() {
  local version="1.9.3"
  local sver="1.9"
  local file="julia-${version}-linux-x86_64.tar.gz"
  curl -fLo "$file" "https://julialang-s3.julialang.org/bin/linux/x64/$sver/$file"
  success "Julia $version is downloaded."
}

download 
